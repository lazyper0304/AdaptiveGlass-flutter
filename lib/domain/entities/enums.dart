library;

/// 枚举类型
enum BlurMode { standard, dark, light }
enum BorderStyle { none, thin, rounded }
enum MonoColor { white, black }
enum WatermarkMode { replace, fallback, append }
enum WatermarkPosition {
  bottomCenter,
  bottomRight,
  bottomLeft,
  topCenter,
  topRight,
  topLeft,
  center,
  centerRight,
  centerLeft,
  manual,
}
enum WatermarkFontFamily { system, smileySans }
enum ColorWalkPosition { left, right, top, bottom }
enum ClassicInfoLogoType { camera, none }
enum RatioPreset {
  square,
  fourThree,
  threeTwo,
  wide,
  portrait,
  cinema,
  original,
}
